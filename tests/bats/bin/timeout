#!/usr/bin/env bash
set -euo pipefail

if [[ $# -lt 2 ]]; then
  echo "usage: timeout DURATION COMMAND [ARG...]" >&2
  exit 1
fi

duration="$1"
shift

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$script_dir/timeout.py" "$duration" "$@"
