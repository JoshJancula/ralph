# Agent-local skills (implementation)

Add skill folders or documentation here for the **implementation** agent only.

Reference paths in `config.json` in the `skills` array relative to the repo root, for example:

`.cursor/agents/implementation/skills/my-skill/SKILL.md`

Teams can add capabilities here without editing shared runner scripts.
