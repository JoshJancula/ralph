Aim for 8-30 TODOs for typical work; if three consecutive TODOs could be done
without re-reading a different file, combine them into one. Every TODO needs its
own executable `verification` command -- a check that the TODO's own change
works, not merely that a file exists or contains a word. Do not invent product
decisions the upstream artifacts did not settle; record the open question
instead. Keep the plan the smallest independently verifiable set of steps that
completes the task.
