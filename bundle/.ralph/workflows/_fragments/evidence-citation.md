Cite provenance for every factual claim. Use `cite: path/to/file.py:42` on its
own line, or inline as `cite:path/to/file.py:42`. Reference an upstream artifact
by anchor, such as `investigation.md#Root-Cause`. A claim about the code with no
citation is treated as unverified by every downstream stage.
