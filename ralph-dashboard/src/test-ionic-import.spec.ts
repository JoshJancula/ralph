import './angular-test-env';
import { IonSpinner } from '@ionic/angular/standalone';

describe('Ionic Import Test', () => {
  it('should import IonSpinner', () => {
    expect(IonSpinner).toBeDefined();
  });
});
