.DS_Store
*.log
.ralph-workspace
/.cursor
/.claude
/.codex
/.ralph
/PLAN.md
/PLAN*.md
/HUMAN_ACTION_REQUIRED.md
/dashboard.orch.json
.agents
!bundle/.agents/
!bundle/.agents/**
# Shipped plugin packages carry their own .agents payload (the Codex and
# Antigravity marketplace manifests). The unanchored rule above is meant for
# the repo-root runtime dir, not for package content that must be committed.
!plugins/**/.agents/
!plugins/**/.agents/**
/.opencode
# Repo-owned runtime roots (project-specific Ralph dev config; not install output)

/.playwright-mcp
__pycache__
# test run temp dirs (created by mcp-server.bats and orchestration tests)
tests/bats/next-todo.*
tests/bats/plan-status.*
tests/bats/mcp-*-fixture-*
tests/bats/mcp-run-plan-*
tests/tmp.wrk/
# ad-hoc root debug scripts
/test_*.sh
.pytest_cache
# Local Claude Code settings (machine-specific permissions)
settings.local.json
docs/cookbook-review
logs